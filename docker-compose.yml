version: "3.3"

services:
  web:
    build:
      context: .
      # dockerfile: Dockerfile   # optional if not default
    # image: trainwithshubham/my-note-app:latest
    ports:
      - "8000:8000"
